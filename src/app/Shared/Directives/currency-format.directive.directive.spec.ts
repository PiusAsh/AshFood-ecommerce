/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { CurrencyFormat.directiveDirective } from './currency-format.directive.directive';

describe('Directive: CurrencyFormat.directive', () => {
  it('should create an instance', () => {
    const directive = new CurrencyFormat.directiveDirective();
    expect(directive).toBeTruthy();
  });
});
